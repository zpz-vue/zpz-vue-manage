export default {
  namespaced: true,
  state: {
    name: '工作台Store'
  },
  mutations: {
    ACCOUNT_AUTH_STATUS_CHANGED: (state, { data }) => {
    }
  },
  actions: {
    // Register
    accountRegisterInit ({ commit }) {
    }
  }
}
